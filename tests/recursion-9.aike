type option<'T> = Some 'T | None

let parseExpr() =
    let a = parseExpr()
    let b: option<int> = parseExpr()

    if true then Some(a) else b

let x = parseExpr()

0

// ERRORS:
// (7,31): Type mismatch. Expecting a
//     option<option<int>>
// but given a
//     option<int>
