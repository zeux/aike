type Node =
| Leaf int
| Tree { left: Node; right: Node }

let x = Tree(Leaf(4), Tree(Leaf(20), Leaf(100)))

let sum(x) =
    match x with
    | Leaf x -> x
    | Tree x -> sum(x.left) + sum(x.right)

sum(x)

// OUTPUT:
// 124
