type Option<'T> =
| Some 'T
| None

let isNone(x) =
    match x with
    | None -> true
    | _ -> false

if isNone(None) then 1 else 2

// ERRORS:
// (10,4): No instance of the generic type 'T found
