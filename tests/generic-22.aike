type Option<'T> =
| Some 'T
| None

let isNone(x) =
    match x with
    | None -> true
    | _ -> false

let y = None

if isNone(y) then 1 else 2

// ERRORS:
// (10,9): No instance of the generic type 'T found
