let foo(x) =
    if true then true
    else bar() == 0

let bar() =
    if foo(1) then 1
    else if foo(true) then 0
    else -1

bar()

// ERRORS:
// (7,17): Type mismatch. Expecting a
//     int
// but given a
//     bool
