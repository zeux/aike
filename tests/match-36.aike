type test = { x: int; y:int[] }

let x = test(1, [2, 4])

match x with
| test(y, [1, 2]) | test(1, y) -> y[1]
| _ -> 0

// ERRORS:
// (6,29): Type mismatch. Expecting a
//     int
// but given a
//     int[]
