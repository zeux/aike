extern printint(x: int): unit

let add(x: int, y: int): int =
    x + y

let div2(n: int): int =
    if n == 0 then
        42
    else
        printint(n)
        n / 2

let main() =
    let x = div2(5) - 1
        x / 4 + 2 * 2 - div2(5)

main() + div2(0) + add(4, 5)

// OUTPUT:
// 5
// 5
// 53
