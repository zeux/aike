extern fn init()
extern fn println(str: string)
extern fn printf(str: string, val: int, val: int)

fn world(dbg: string, print: bool): string
    if print
        println(dbg)
    "world"

fn main(): int
    init()
    var hello: string = "Hello, "
    var foo: bool = false
    println(hello)
    println(world("test", false))
    printf("%d %d%c", 5, 10)
    0