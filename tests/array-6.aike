let length(x) =
    builtin(arrayLength, x)

let sum(arr) =
    let loop(offset, acc) =
        if offset < arr#length then
            loop(offset + 1, arr[offset] + acc)
        else
            acc
    loop(0, 0)

let x = [2, 3]

sum(x)

// OUTPUT:
// 5
