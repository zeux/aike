type Integer =
| Zero
| Succ Integer

type NonInteger =
| NaN

let value(x) =
    match x with
    | Zero -> 0
    | NaN -> 1

value(Succ(Zero))

// ERRORS:
// (11,7): Type mismatch. Expecting a
//     Integer
// but given a
//     NonInteger
