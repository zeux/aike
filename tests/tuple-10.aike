type Shape = Circle (int) | Rectangle (int, int)

let collide(a, b) =
  match (a, b) with
    | (Circle x, Circle y) -> 0
    | (Circle x, Rectangle y) | (Rectangle y, Circle x) -> 1
    | (Rectangle x, Rectangle y) -> 0

collide(Circle(5), Rectangle((3, 4)))

// OUTPUT:
// 1
