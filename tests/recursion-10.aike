type option<'T> = Some 'T | None
type Choice = Choice1 int | Choice2 option<Choice>

let parseTerm() =
    Choice1(0)

let parseExpr() =
    let a = parseTerm()
    let b = parseExpr()

    if true then a else Choice2(b)

let x = parseExpr()

0

// ERRORS:
// (8,5): Type mismatch. Expecting a
//     option<Choice>
// but given a
//     Choice
